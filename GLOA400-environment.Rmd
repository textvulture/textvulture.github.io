---
title: "Environmental Issues in Asia <br> GLOA 400-001"
author: "Byunghwan Son <br> George Mason University"
date: " "
#########################################
linkcolor: red
citecolor: blue
urlcolor: blueß
output: 
  xaringan::moon_reader:
    seal: false
    nature:
      ratio: '16:9'
      highlightStyle: github
  #    self-contained: TRUE
  chakra: css/libs/remark-latest.min.js
css: ["default", 
      "css/footer.css", 
      "css/color.css", 
      # "css/xaringan-themer.css",
      default-fonts]
lib_dir: css/libs
beforeInit: "css/macros.js"
# seal: false  
#bibliography: /Users/bson3/Dropbox/latex/mybib2.bib
#remotes::install_github('yihui/xaringan', upgrade = TRUE)
---

```{r setup, include=FALSE}
# to use FontAwesome
#htmltools::tagList(rmarkdown::html_dependency_font_awesome())
library(fs)
library(fontawesome)
# library(magick)  
library(ggplot2)
library(tidyverse)
```

class: inverse, right, top
background-image: url(https://research.tamu.edu/wp-content/uploads/2014/01/AirPollution2.jpg)
background-size: cover

# .huge[**Environmental Issues <br> in Asia <br> GLOA 400-001**]

---



```{r include=F}

library(WDI)

var = c('CO2' = 'NY.ADJ.DCO2.GN.ZS',
        'air' = 'EN.ATM.PM25.MC.M3')

wdi <- WDI(indicator=var,
           start=1970,
           country = 'all',
           end=2020,
           extra=TRUE)

wdi %>%
  filter(region !="Aggregates") %>%
  mutate(Asia = 
           case_when(
             region == "East Asia & Pacific" & 
               country != "China" & 
               country != "India" &
               country != "Mongolia" ~ "Other Asia",
             country == "China" ~ "China",
             country == "India" ~ "India",
             country == "Mongolia" ~ "Mongolia",
             country != "East Asia & Pacific" ~ "Others"
           )) %>%
  filter(Asia != "Others")-> wdi2
  
wdi %>%
  filter(region !="Aggregates") %>%
  mutate(Asia = 
           case_when(
             region == "East Asia & Pacific" & 
               country != "China" & 
               country != "India" &
               country != "Mongolia" ~ "Other Asia",
             country == "China" ~ "China",
             country == "India" ~ "India",
             country == "Mongolia" ~ "Mongolia",
             country != "East Asia & Pacific" ~ "Others"
           )) %>%
  filter(Asia == "Others")-> wdi3
```


<div class="full-width-tinted"><br>
  <h1> .large[Air Pollution] </h1> 
</div><br><br> 


```{r echo=F, message=F, warning=F, error=F, cache=T, fig.width=16}

  ggplot(wdi2, aes(x=year,
             y=air
             )) +
  geom_point(data=wdi2, aes(color=Asia), alpha=1, size=2) +
  geom_point(data=wdi3, size=0.15, alpha=0.25, color='black') + 
  labs(x = "YEAR",
       y = "Air Pollution (mg per cubic meter)") +
  theme_minimal() +
  theme(legend.title=element_text(size=15),
        legend.text=element_text(size=15),
        axis.text=element_text(size=15),
        axis.title=element_text(size=15)) +
  xlim(1990, 2020)


```

---
<div class="full-width-tinted"><br>
  <h1> $$ \text{Economic Effect: } CO_2 \text{          } \text{Damage, % of GNI} $$ </h1> 
</div><br><br> 

```{r echo=F, message=F, warning=F, error=F, cache=T, fig.width=16}

  ggplot(wdi2, aes(x=year,
             y=CO2
             )) +
  geom_point(data=wdi2, aes(color=Asia), alpha=1, size=2) +
  geom_point(data=wdi3, size=0.15, alpha=0.25, color='black') + 
  labs(x = "YEAR",
       y = "Economic Cost of CO2 (% GNI)") +
  theme_minimal() +
  theme(legend.title=element_text(size=15),
        legend.text=element_text(size=15),
        axis.text=element_text(size=15),
        axis.title=element_text(size=15))

```


---
